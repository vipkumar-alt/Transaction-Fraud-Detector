version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: fraud-rag-postgres
    environment:
      POSTGRES_DB: fraud_rag_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: